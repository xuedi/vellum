# Claude Guidelines for vallum

## Project Overview

Rust static site generator transforming a Markdown file into self-contained HTML with embedded assets.

**Repo**: `git@github.com:xuedi/vallum.git`

### Core Principles
- Self-contained output: CSS, JS, logo (base64) all inline
- Offline-capable: No external dependencies
